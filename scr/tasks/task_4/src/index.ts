$(".slider").slick({
    infinite: true,
    autoplay: true,
    autoplaySpeed: 1000,
    dots: true,
    arrows: true,
})

// TODO: Home task:
//     in module mode
// HomeContainer
// dom element access class variables (1)
// in constartor get the access from (1)
//
// Ascending/Descending etc.            -> enum SortType    +
// create a class with visiblity none   -> popup-hiden      +
// in change the the css class
// save state pop up is hide/not
// save state of filter mode
// save state of settings mode